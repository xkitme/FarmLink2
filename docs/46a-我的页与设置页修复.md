# 分段 46a — 我的页修复 + 真设置中心（按设计稿重写）

> 工作单（Codex 实施）。所属批次：[46-用户反馈批次](46-用户反馈批次.md)。
> 解决用户反馈 1（双刷新 + 无感刷新）+ 反馈 3（点设置只 toast）。
>
> 设计稿来自 `D:\dgitc_project\InkFlow\design_ref\设置页\`，共 7 张：`_1`~`_7`。
> 本段做 5 个完整子页 + 2 个占位子页；个人资料 `_7` 与存储管理 `_3` 拆到分段 46d 单独做。

## 一、目标

1. **我的页**：去 AppBar refresh / 数据同步卡静默自动 / 「设置」跳到真设置中心
2. **设置中心 8 个路由**：
   - `/profile/settings` —— 设置主页（设计稿 `_1`）
   - `/profile/settings/account` —— 个人资料（设计稿 `_7`，**本段做跳板占位，46d 补完**）
   - `/profile/settings/password` —— 修改密码（设计稿 `_5`）
   - `/profile/settings/push` —— 消息推送（设计稿 `_6`）
   - `/profile/settings/weather` —— 气象预警提醒（设计稿 `_4`）
   - `/profile/settings/storage` —— 存储空间管理（设计稿 `_3`，**本段做跳板占位，46d 补完**）
   - `/profile/settings/about` —— 关于田园通（设计稿 `_2`）
   - `/profile/settings/privacy` —— 隐私设置（无设计稿，做简洁开关版）
   - `/profile/settings/help` —— 帮助与反馈（无设计稿，复用 `POST /feedback`）

## 二、改动清单

### Part A · 我的页（`app/lib/pages/profile/profile_page.dart`）

#### A.1 去 AppBar refresh

```dart
// 删除
appBar: FarmAppBar(
  actions: [
    IconButton(
      onPressed: _loading ? null : _load,
      icon: const Icon(Icons.refresh, color: AppColors.onSurfaceVariant),
    ),
  ],
),
// 改为
appBar: const FarmAppBar(),
```

`RefreshIndicator` 已包裹 ListView，下拉刷新就够用。

#### A.2 数据同步卡：自动 + 静默

state 新增：

```dart
bool _autoSyncing = false;
```

`_load` 末尾追加：

```dart
final waiting = _queue.where((item) => item.status != SyncStatus.synced).length;
if (waiting > 0 && !_autoSyncing) {
  _autoSyncSilently();
}
```

新增静默同步方法：

```dart
Future<void> _autoSyncSilently() async {
  if (!mounted) return;
  setState(() => _autoSyncing = true);
  try {
    await OfflineSyncQueue.flush();
  } catch (_) {
    // 静默吞，结果通过下次 _load 反映
  }
  if (!mounted) return;
  setState(() => _autoSyncing = false);
  await _load();
}
```

`_syncPanel` 重写（删除「同步」按钮 → StatusChip + 底部 LinearProgress）：

```dart
Widget _syncPanel() {
  final waiting =
      _queue.where((item) => item.status != SyncStatus.synced).length;
  final failed = _int(_syncStatus['failed']);
  final conflict = _int(_syncStatus['conflict']);
  final total = _int(_syncStatus['total']);

  String stateLabel;
  Color stateColor;
  if (_autoSyncing) {
    stateLabel = '同步中';
    stateColor = AppColors.primary;
  } else if (waiting > 0) {
    stateLabel = '待发送 $waiting';
    stateColor = AppColors.warning;
  } else if (failed > 0 || conflict > 0) {
    stateLabel = '失败 ${failed + conflict}';
    stateColor = AppColors.error;
  } else {
    stateLabel = '已同步';
    stateColor = AppColors.primary;
  }

  return AppCard(
    padding: EdgeInsets.zero,
    onTap: () => context.go('/data/service'),
    child: Column(
      children: [
        Padding(
          padding: const EdgeInsets.all(16),
          child: Row(
            children: [
              Container(
                width: 44, height: 44,
                decoration: BoxDecoration(
                  color: AppColors.primaryContainer.withValues(alpha: 0.12),
                  borderRadius: BorderRadius.circular(R.md),
                ),
                child: const Icon(Icons.sync_alt_rounded, color: AppColors.primary),
              ),
              const SizedBox(width: 12),
              Expanded(
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    const Text('数据同步',
                        style: TextStyle(
                            fontSize: 16, fontWeight: FontWeight.w700,
                            color: AppColors.onSurface)),
                    const SizedBox(height: 3),
                    Text(
                      '已同步 $total 条，待发送 $waiting 条',
                      maxLines: 2, overflow: TextOverflow.ellipsis,
                      style: const TextStyle(color: AppColors.onSurfaceVariant),
                    ),
                  ],
                ),
              ),
              const SizedBox(width: 8),
              StatusChip(stateLabel, color: stateColor),
            ],
          ),
        ),
        if (_autoSyncing)
          const LinearProgressIndicator(
            minHeight: 2,
            backgroundColor: Colors.transparent,
            color: AppColors.primary,
          ),
      ],
    ),
  );
}
```

#### A.3 设置入口

```dart
// 删除
_menuAction(context, Icons.elderly, '适老模式', () => toast(...)),
_menuAction(context, Icons.settings_outlined, '设置', () => toast(...)),
// 改为
_menu(context, Icons.settings_outlined, '设置', '/profile/settings'),
```

「适老模式」从我的页移走，并入设置中心的「显示偏好」（设计稿 `_1` 没明示，本工作单**先不加适老模式开关**，等 46d 个人资料页一起做，避免散落）。
我的页菜单去掉「适老模式」一项即可。

### Part B · 设置主页（`_1` 复刻）

新建 `app/lib/pages/profile/settings/settings_home_page.dart`：

按 `_1` 设计稿 1:1 复刻：

- AppBar：返回箭头 + 「设置」标题（绿色 w700）
- **账号与安全** 分组（小灰字标签）
  - 顶部卡：圆头像 + 用户名（粗体）+「个人资料」小字 + chevron → 跳 `/profile/settings/account`
  - 列表项：手机号绑定（脱敏 `138****8888` + chevron）→ 跳 `/profile/settings/account`（占位，本段不做单独绑定页）
  - 列表项：修改密码 → 跳 `/profile/settings/password`
- **通知与通用** 分组
  - 消息推送设置 → 跳 `/profile/settings/push`
  - 气象预警提醒 → 跳 `/profile/settings/weather`
  - 语言设置（显示「简体中文」+ chevron）→ 暂 toast「当前仅支持简体中文」
  - 存储空间管理 → 跳 `/profile/settings/storage`
- **隐私与支持** 分组
  - 隐私设置 → 跳 `/profile/settings/privacy`
  - 清除缓存（右侧显示估算大小，本段简化为 `_cacheSizeLabel` 静态值；46d 接真值）→ 调 `_clearCache()` toast 清理结果
  - 帮助与反馈 → 跳 `/profile/settings/help`
  - 关于田园通（右侧显示 v1.0.0）→ 跳 `/profile/settings/about`
- 底部 OutlinedButton：红框 +「退出登录」+ logout 跳 `/auth/login`
- 最底小灰字：「农业技术驱动，连接每一寸土地」

视觉规则：

- 每个分组 = 一张 AppCard（padding: EdgeInsets.zero），内 ListTile + Divider(indent: 56)
- 用户头像卡片是分组第一张，单独样式（避免与下面 ListTile 视觉重复）

代码骨架：

```dart
class SettingsHomePage extends StatelessWidget {
  const SettingsHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    final auth = context.watch<AuthState>();
    final user = auth.user;
    return Scaffold(
      backgroundColor: AppColors.background,
      appBar: AppBar(
        backgroundColor: AppColors.background,
        elevation: 0,
        leading: IconButton(
          icon: const Icon(Icons.arrow_back, color: AppColors.primary),
          onPressed: () =>
              context.canPop() ? context.pop() : context.go('/profile'),
        ),
        title: const Text('设置',
            style: TextStyle(
                color: AppColors.primary,
                fontSize: 20,
                fontWeight: FontWeight.w700)),
      ),
      body: ListView(
        padding: const EdgeInsets.fromLTRB(16, 8, 16, 28),
        children: [
          const _GroupLabel('账号与安全'),
          AppCard(
            padding: EdgeInsets.zero,
            child: Column(
              children: [
                _ProfileTile(user: user, onTap: () => context.go('/profile/settings/account')),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.phone_iphone,
                  label: '手机号绑定',
                  trailingText: _maskPhone(user?.phone),
                  onTap: () => context.go('/profile/settings/account'),
                ),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.lock_outline,
                  label: '修改密码',
                  onTap: () => context.go('/profile/settings/password'),
                ),
              ],
            ),
          ),

          const _GroupLabel('通知与通用'),
          AppCard(
            padding: EdgeInsets.zero,
            child: Column(
              children: [
                _SettingTile(
                  icon: Icons.notifications_outlined,
                  label: '消息推送设置',
                  onTap: () => context.go('/profile/settings/push'),
                ),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.thunderstorm_outlined,
                  label: '气象预警提醒',
                  onTap: () => context.go('/profile/settings/weather'),
                ),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.language,
                  label: '语言设置',
                  trailingText: '简体中文',
                  onTap: () => toast(context, '当前仅支持简体中文'),
                ),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.storage_outlined,
                  label: '存储空间管理',
                  onTap: () => context.go('/profile/settings/storage'),
                ),
              ],
            ),
          ),

          const _GroupLabel('隐私与支持'),
          AppCard(
            padding: EdgeInsets.zero,
            child: Column(
              children: [
                _SettingTile(
                  icon: Icons.shield_outlined,
                  label: '隐私设置',
                  onTap: () => context.go('/profile/settings/privacy'),
                ),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.cleaning_services_outlined,
                  label: '清除缓存',
                  trailingText: '清理',
                  onTap: () => _clearCache(context),
                ),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.help_outline,
                  label: '帮助与反馈',
                  onTap: () => context.go('/profile/settings/help'),
                ),
                const Divider(height: 1, indent: 56),
                _SettingTile(
                  icon: Icons.info_outline,
                  label: '关于田园通',
                  trailingText: 'v1.0.0',
                  onTap: () => context.go('/profile/settings/about'),
                ),
              ],
            ),
          ),

          const SizedBox(height: 32),
          OutlinedButton.icon(
            onPressed: () async {
              await auth.logout();
              if (context.mounted) context.go('/auth/login');
            },
            icon: const Icon(Icons.logout_rounded, size: 18),
            label: const Text('退出登录'),
            style: OutlinedButton.styleFrom(
              foregroundColor: AppColors.error,
              side: const BorderSide(color: AppColors.error, width: 2),
              minimumSize: const Size.fromHeight(52),
            ),
          ),
          const SizedBox(height: 16),
          const Center(
            child: Text('农业技术驱动，连接每一寸土地',
                style: TextStyle(
                    fontSize: 12, color: AppColors.onSurfaceVariant)),
          ),
        ],
      ),
    );
  }

  String _maskPhone(String? phone) {
    if (phone == null || phone.length < 7) return '未绑定';
    return '${phone.substring(0, 3)}****${phone.substring(phone.length - 4)}';
  }

  Future<void> _clearCache(BuildContext context) async {
    final sp = await SharedPreferences.getInstance();
    final keys = sp.getKeys().where((k) => k.startsWith('cache:')).toList();
    for (final k in keys) {
      await sp.remove(k);
    }
    if (context.mounted) {
      toast(context, '本地缓存已清理（${keys.length} 项）');
    }
  }
}

class _GroupLabel extends StatelessWidget {
  final String text;
  const _GroupLabel(this.text);
  @override
  Widget build(BuildContext context) => Padding(
        padding: const EdgeInsets.fromLTRB(4, 18, 4, 8),
        child: Text(text,
            style: const TextStyle(
                fontSize: 13,
                color: AppColors.onSurfaceVariant,
                fontWeight: FontWeight.w600)),
      );
}

class _ProfileTile extends StatelessWidget {
  final dynamic user;
  final VoidCallback onTap;
  const _ProfileTile({required this.user, required this.onTap});
  @override
  Widget build(BuildContext context) => ListTile(
        onTap: onTap,
        leading: CircleAvatar(
          radius: 22,
          backgroundColor: AppColors.primaryContainer.withValues(alpha: 0.20),
          child: const Icon(Icons.person, color: AppColors.primary, size: 26),
        ),
        title: Text(user?.displayName ?? '未登录',
            style: const TextStyle(
                fontSize: 17, fontWeight: FontWeight.w700,
                color: AppColors.onSurface)),
        subtitle: const Text('个人资料',
            style: TextStyle(
                fontSize: 12, color: AppColors.onSurfaceVariant)),
        trailing: const Icon(Icons.chevron_right, color: AppColors.outline),
      );
}

class _SettingTile extends StatelessWidget {
  final IconData icon;
  final String label;
  final String? trailingText;
  final VoidCallback onTap;
  const _SettingTile({
    required this.icon,
    required this.label,
    this.trailingText,
    required this.onTap,
  });
  @override
  Widget build(BuildContext context) => ListTile(
        leading: Icon(icon, color: AppColors.primary, size: 22),
        title: Text(label, style: const TextStyle(fontSize: 15)),
        trailing: Row(
          mainAxisSize: MainAxisSize.min,
          children: [
            if (trailingText != null)
              Text(trailingText!,
                  style: const TextStyle(
                      fontSize: 13, color: AppColors.onSurfaceVariant)),
            const SizedBox(width: 4),
            const Icon(Icons.chevron_right, color: AppColors.outline),
          ],
        ),
        onTap: onTap,
      );
}
```

### Part C · 修改密码（`_5` 复刻）

新建 `app/lib/pages/profile/settings/password_page.dart`：

布局：

- AppBar 返回 + 「修改密码」
- 顶部 banner 图（暂用 `assets/images/_6_1.jpg` 或随便一张田野图，圆角 16）
- 白卡：3 个输入框
  - 当前密码（obscure + 眼睛切换）
  - 新密码（obscure + 眼睛 + 下方小灰字「密码应包含字母、数字及特殊符号」）
  - 确认新密码（obscure + 眼睛）
  - 右下「忘记密码？」TextButton → toast「请联系村委或管理员重置密码」
- 底部 ElevatedButton 满宽：「保存修改」+ 落地后跳回 + toast
- 最底小灰字：「FarmLink 安全中心保护您的隐私」

行为：

```dart
Future<void> _submit() async {
  if (_newPwd.text.length < 8) {
    toast(context, '新密码至少 8 位', error: true);
    return;
  }
  if (_newPwd.text != _confirm.text) {
    toast(context, '两次新密码不一致', error: true);
    return;
  }
  setState(() => _saving = true);
  try {
    await ApiClient.put('/user/password', body: {
      'oldPassword': _oldPwd.text,
      'newPassword': _newPwd.text,
    });
    if (!mounted) return;
    toast(context, '密码已修改，请重新登录');
    await context.read<AuthState>().logout();
    if (!mounted) return;
    context.go('/auth/login');
  } catch (e) {
    if (mounted) toast(context, actionErrorMessage('修改', e), error: true);
  } finally {
    if (mounted) setState(() => _saving = false);
  }
}
```

### Part D · 消息推送（`_6` 复刻）

新建 `app/lib/pages/profile/settings/push_settings_page.dart`：

按 `_6` 设计稿：

- AppBar：返回 + 「消息推送设置」
- 绿色 hero 卡：标题「通知中心」+ 副标「及时获取农场最新动态与专业建议」+ 大铃铛图标
- 白卡：4 个 SwitchListTile
  - 系统消息（开 / 关于账号安全与系统维护的通知）
  - 农技建议提醒（开 / 针对当前节气的精准种植指导）
  - 社区动态通知（关 / 关注的农友动态或互动提醒）
  - 活动提醒（开 / 线上讲座暨线下农机展会活动）
- 底部 hint 卡（浅色背景）：「关闭通知可能会导致错过重要的生产指导或灾害预警，建议保留「系统消息」与「农技建议提醒」。」
- 底部脚标：「FarmLink 智慧农业互联平台」

每个 Switch 的状态 SharedPreferences key：`push.system / push.farm / push.community / push.event`，默认值（true / true / false / true）。

### Part E · 气象预警提醒（`_4` 复刻）

新建 `app/lib/pages/profile/settings/weather_alert_page.dart`：

按 `_4` 设计稿：

- AppBar：返回 + 「气象预警提醒」
- 顶部 banner 图（田野日落）+ overlay 文字「配置您的精准农业气象监控」
- 「预警类型」分组：
  - 暴雨预警（开） + 「降水量超阈值或值提醒」
  - 高温预警（开） + 「超过 35℃ 极端气温预警」
  - 霜冻预警（关） + 「作物冻害风险提示」
  - 台风预警（开） + 「风力等级及路径追踪」
- 「通知频率」分组（2 个卡片切换，单选）：
  - 实时推送（默认选中，绿色边框 + 勾） + 「即时掌握动态」
  - 每日总结 + 「每日 18:00 推送」
- 浅绿提示卡：「温馨提示：开启实时预警后，系统将结合您的农场地理位置（GPS）进行精准下发，请确保应用拥有通知权限。」
- 底部两个按钮：
  - 主：「保存设置」ElevatedButton 满宽
  - 次：「恢复默认配置」OutlinedButton 满宽

SharedPreferences key：`alert.rain / alert.heat / alert.frost / alert.typhoon / alert.freq`（freq 取 'realtime' / 'daily'）。

### Part F · 关于田园通（`_2` 复刻）

新建 `app/lib/pages/profile/settings/about_page.dart`：

按 `_2` 设计稿：

- AppBar：返回 + 「关于田园通」
- 中央大圆角绿底卡：App 图标（拖拉机绿底白icon）
- 「田园通 FarmLink」大字 + 「版本 v1.0.0」小灰字
- 绿色圆胶囊「检查更新」按钮 → toast「已是最新版本」
- 白卡：3 个 ListTile
  - 服务协议 → 弹 sheet 显示长文本占位（标题 + 5 段 Lorem）
  - 隐私政策 → 同上
  - 官网站点 → 显示外链图标（不实际跳转，toast「请访问 https://farmlink.example.com」）
- 浅米色背景卡：「田园通致力于为现代农业提供智能化解决方案。通过 AI 土壤分析、智能排灌与实时气候监测，我们助力每一位耕耘者实现科技助农、丰收万家。」
- 底部居中小灰字：
  - 「© 2026 田园通科技 (FarmLink Tech)」
  - 「All Rights Reserved. 粤 ICP 备 88888888 号」

### Part G · 隐私设置（无设计稿）

新建 `app/lib/pages/profile/settings/privacy_settings_page.dart`：

简洁版，3 个分组：

- 个性化推荐（开关）：「基于您的农事记录与查询历史提供个性化政策与商品推荐」
- 数据贡献（开关）：「向平台提供脱敏后的种植数据用于改进 AI 推荐算法」
- 设备权限（按钮）：「位置权限」「相机权限」「通知权限」—— 点击 toast「请在系统设置中调整」

### Part H · 帮助与反馈（无设计稿）

新建 `app/lib/pages/profile/settings/help_feedback_page.dart`：

- AppBar：返回 + 「帮助与反馈」
- 顶部「常见问题」卡：4 个 ExpansionTile（怎么发布动态 / AI 识别不准怎么办 / 如何申请补贴 / 数据同步失败）
- 中部「联系我们」卡：客服电话（toast 显示），客服邮箱（toast）
- 底部「提交反馈」表单：textarea + 「提交」按钮 → `POST /feedback`，body 含 `content` 字段（参考现有 `/feedback` 路由）

### Part I · 个人资料 / 存储管理（占位）

新建两个占位页（**这两个 46d 单独细化补完**）：

`app/lib/pages/profile/settings/account_page.dart`：

```dart
@override
Widget build(BuildContext context) => Scaffold(
  appBar: AppBar(/* 返回 + 「个人资料」 */),
  body: Center(
    child: Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        const Icon(Icons.engineering, size: 64, color: AppColors.outline),
        const SizedBox(height: 12),
        const Text('个人资料完整编辑\n开发中', textAlign: TextAlign.center),
        const SizedBox(height: 8),
        const Text('（农场名称 / 主要作物 / 性别等字段待后端补 schema）',
            style: TextStyle(fontSize: 12, color: AppColors.outline)),
      ],
    ),
  ),
);
```

`app/lib/pages/profile/settings/storage_page.dart`：同结构，文案改「存储空间管理 / 开发中（46d 完成）」。

> **不要**因为做占位而省略路由注册。占位是为了让用户从设置主页能点进来不报 404；46d 接手时只需替换内容。

### Part J · 后端新接口 `PUT /user/password`

`backend/src/modules/platform/user.controller.js` 新增：

```js
import bcrypt from 'bcryptjs'

/** 修改密码 */
export async function updatePassword(req, res) {
  const { oldPassword, newPassword } = req.body
  if (!oldPassword || !newPassword) {
    throw errors.param('请填写当前密码与新密码')
  }
  if (newPassword.length < 8) {
    throw errors.param('新密码至少 8 位')
  }
  const user = await prisma.user.findUnique({ where: { id: req.user.id } })
  if (!user) throw errors.notFound('用户不存在')
  const isOk = await bcrypt.compare(oldPassword, user.passwordHash)
  if (!isOk) throw errors.param('当前密码不正确')
  const newHash = await bcrypt.hash(newPassword, 10)
  await prisma.user.update({
    where: { id: req.user.id },
    data: { passwordHash: newHash },
  })
  ok(res, { ok: true }, '密码已修改，请重新登录')
}
```

`backend/src/modules/platform/platform.routes.js` 注册：

```js
router.put('/user/password', requireAuth, wrap(user.updatePassword))
```

> Codex 实施时确认 `bcryptjs` 已在 dependencies（auth.controller 已用），无需新装。

### Part K · 路由（`app/lib/core/router.dart`）

```dart
import '../pages/profile/settings/settings_home_page.dart';
import '../pages/profile/settings/account_page.dart';
import '../pages/profile/settings/password_page.dart';
import '../pages/profile/settings/push_settings_page.dart';
import '../pages/profile/settings/weather_alert_page.dart';
import '../pages/profile/settings/storage_page.dart';
import '../pages/profile/settings/about_page.dart';
import '../pages/profile/settings/privacy_settings_page.dart';
import '../pages/profile/settings/help_feedback_page.dart';

// ShellRoute.routes 内追加 9 条：
GoRoute(path: '/profile/settings', builder: (_, __) => const SettingsHomePage()),
GoRoute(path: '/profile/settings/account', builder: (_, __) => const AccountPage()),
GoRoute(path: '/profile/settings/password', builder: (_, __) => const PasswordPage()),
GoRoute(path: '/profile/settings/push', builder: (_, __) => const PushSettingsPage()),
GoRoute(path: '/profile/settings/weather', builder: (_, __) => const WeatherAlertPage()),
GoRoute(path: '/profile/settings/storage', builder: (_, __) => const StoragePage()),
GoRoute(path: '/profile/settings/about', builder: (_, __) => const AboutPage()),
GoRoute(path: '/profile/settings/privacy', builder: (_, __) => const PrivacySettingsPage()),
GoRoute(path: '/profile/settings/help', builder: (_, __) => const HelpFeedbackPage()),
```

## 三、接口契约

| 路径 | 方法 | 用途 | 状态 |
|---|---|---|---|
| `/user/profile` | GET / PUT | 个人资料（46d 完整对接） | 已有 |
| `/user/password` | PUT | 修改密码 | **本段新增** |
| `/feedback` | POST | 帮助反馈 | 已有 |

其它子页（推送 / 气象 / 隐私 / 存储 / 关于）**全部本地 SharedPreferences**，无网络。

## 四、视觉规则

- 设置子页 AppBar 统一：白底（`AppColors.background`）+ 绿色返回箭头 + 绿色 w700 标题
- 分组小灰字 13px w600 `onSurfaceVariant`
- 列表项 ListTile + Divider(indent: 56)
- 主 CTA：满宽 ElevatedButton 高 52
- 次 CTA：满宽 OutlinedButton 高 52
- 危险动作（退出/删除）：红框 OutlinedButton

## 五、验收

1. `flutter analyze lib/pages/profile lib/core/router.dart` → `No issues found!`
2. 后端 `node --check user.controller.js platform.routes.js` 通过
3. 启动端到端：
   - 进入「我的」→ AppBar 右上**无 refresh**；下拉刷新生效；同步卡**无按钮**，有 StatusChip + 进度条
   - 点「设置」→ 进入设置主页，看到 3 个分组（账号与安全 / 通知与通用 / 隐私与支持）
   - 9 个入口逐个能跳到对应子页（个人资料 / 存储管理是占位但能进去）
   - 修改密码：填错当前密码 → toast「当前密码不正确」；填对 + 新旧不同 → 跳登录页
   - 推送 / 气象预警：4 个开关切换 → 重启 App 保留
   - 关于田园通：版本号显示 v1.0.0，「检查更新」toast「已是最新版本」
   - 帮助反馈：提交内容 → POST /feedback 200 → toast「已收到反馈」
   - 清除缓存：toast 显示清理项数
   - 底部「退出登录」红框按钮 → 跳登录页

## 六、不在范围内（明确写在工作单）

- **个人资料完整编辑（_7）** —— 拆到 46d，本段只做跳板占位
- **存储空间管理真实数据（_3）** —— 拆到 46d，本段只做跳板占位
- 适老模式开关（视觉响应）—— 46d 一起
- 后端 schema 加 gender / mainCrops / farmName 字段 —— 46d
- 手机号修改 / 重新绑定 —— 安全敏感，留后续
- 语言切换实现 —— 当前仅简体中文，UI 写死

## 七、关联

- 总览：[46-用户反馈批次.md](46-用户反馈批次.md)
- 同批次：[46b-AI双层重构.md](46b-AI双层重构.md) / [46c-发布页扩充.md](46c-发布页扩充.md)
- 后续：46d 个人资料完整版 + 存储管理真值（待 Claude 写）
- 设计稿：`design_ref/设置页/_1`~`_7`

## 八、实施备注（Codex 完成后填写）

- 我的页：移除 AppBar 手动刷新按钮，保留下拉刷新；数据同步卡改为自动静默处理待发送队列，卡片点击进入数据管理服务页。
- 设置中心：新增 `/profile/settings` 及账号、密码、推送、气象、存储、关于、隐私、帮助反馈 8 个子页；个人资料与存储管理按本工作单要求先做占位页。
- 后端：新增 `PUT /user/password`，校验当前密码、新密码长度并更新 `passwordHash`，修改成功后前端退出到登录页。
- 文案：新增/修改的用户可见文案已按 `docs/产品呈现口径.md` 第五节校对，未使用「离线 / 断网 / 本地兜底」等词。
- 验证：`node --check backend/src/modules/platform/user.controller.js backend/src/modules/platform/platform.routes.js` 通过；`flutter analyze lib/pages/profile lib/core/router.dart` 通过；`flutter analyze lib` 通过。
