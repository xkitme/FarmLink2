# 分段 46c — 发布页扩充（详情查看 + 多类型）

> 工作单（Codex 实施）。所属批次：[46-用户反馈批次](46-用户反馈批次.md)。
> 解决反馈 4（卡片不能点详情）+ 反馈 5（发布类型太少）。

## 一、目标

1. **卡片点击 → 详情 sheet**：完整内容 + 作者 + 时间 + 标签 + 联系方式 + 操作按钮（联系 TA / 我能帮 TA / 删除自己的）
2. **composer 扩充发布类型**：从 4 种（求助/互助/分享/招工）扩到 **6 种社交类**（互助求助 / 招工 / 分享见闻 / 失物招领 / 二手交易 / 闲置共享）+ 顶部 2 个**特殊上报快捷入口**（灾情上报 / 农事记录，跳转到对应板块的现有 sheet）
3. **feed 显示来源依然 `/life/help/list`** —— 不动后端 union，避免大改

## 二、改动清单

### 1. `app/lib/pages/publish/publish_page.dart`

#### 1.1 卡片点击 → 详情 sheet

`_postCard` 外层加 `Material + InkWell`，onTap 调 `_openDetail(p)`：

```dart
Widget _postCard(Map<String, dynamic> p) {
  // ...原内容包成 Container 不变...
  return Material(
    color: Colors.transparent,
    child: InkWell(
      borderRadius: BorderRadius.circular(R.md),
      onTap: () => _openDetail(p),
      child: Container(...原内容...),
    ),
  );
}

Future<void> _openDetail(Map<String, dynamic> p) async {
  await showModalBottomSheet<void>(
    context: context,
    isScrollControlled: true,
    backgroundColor: AppColors.surface,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.vertical(top: Radius.circular(R.lg)),
    ),
    builder: (ctx) => _PostDetailSheet(post: p, onAfter: _load),
  );
}
```

#### 1.2 新建 `_PostDetailSheet` widget（同文件内）

```dart
class _PostDetailSheet extends StatefulWidget {
  final Map<String, dynamic> post;
  final Future<void> Function() onAfter;
  const _PostDetailSheet({required this.post, required this.onAfter});

  @override
  State<_PostDetailSheet> createState() => _PostDetailSheetState();
}

class _PostDetailSheetState extends State<_PostDetailSheet> {
  bool _accepting = false;

  Future<void> _accept() async {
    final id = widget.post['id'];
    if (id == null) return;
    setState(() => _accepting = true);
    try {
      await ApiClient.post('/life/help/$id/accept');
      if (!mounted) return;
      toast(context, '已响应，感谢您的帮助');
      Navigator.pop(context);
      await widget.onAfter();
    } catch (e) {
      if (mounted) toast(context, actionErrorMessage('响应', e), error: true);
    } finally {
      if (mounted) setState(() => _accepting = false);
    }
  }

  @override
  Widget build(BuildContext context) {
    final p = widget.post;
    final type = '${p['type'] ?? '互助'}';
    final title = '${p['title'] ?? '-'}';
    final content = '${p['content'] ?? ''}';
    final phone = '${p['contactPhone'] ?? ''}';
    final status = '${p['status'] ?? 'OPEN'}';
    final author = '${p['publisherName'] ?? p['userName'] ?? '匿名村民'}';
    final time = '${p['createdAt'] ?? ''}';

    return DraggableScrollableSheet(
      expand: false,
      initialChildSize: 0.6,
      minChildSize: 0.4,
      maxChildSize: 0.92,
      builder: (ctx, scrollCtrl) => ListView(
        controller: scrollCtrl,
        padding: const EdgeInsets.fromLTRB(20, 14, 20, 28),
        children: [
          Center(
            child: Container(
              width: 36, height: 4,
              decoration: BoxDecoration(
                color: AppColors.outlineVariant,
                borderRadius: BorderRadius.circular(2),
              ),
            ),
          ),
          const SizedBox(height: 14),
          // 顶部：类型 chip + 状态 chip
          Row(
            children: [
              StatusChip(type, color: _typeColor(type)),
              const SizedBox(width: 8),
              StatusChip(status == 'DONE' ? '已响应' : '进行中',
                  color: status == 'DONE'
                      ? AppColors.onSurfaceVariant
                      : AppColors.primary),
            ],
          ),
          const SizedBox(height: 14),
          // 标题
          Text(title,
              style: const TextStyle(
                  fontSize: 22,
                  fontWeight: FontWeight.w800,
                  color: AppColors.onSurface)),
          const SizedBox(height: 6),
          Text('$author · ${_friendlyTime(time)}',
              style: const TextStyle(
                  fontSize: 12, color: AppColors.onSurfaceVariant)),
          const SizedBox(height: 18),
          // 内容
          Text(content.isEmpty ? '（暂无详细描述）' : content,
              style: const TextStyle(
                  fontSize: 15, height: 1.6, color: AppColors.onSurface)),
          const SizedBox(height: 24),
          if (phone.isNotEmpty)
            AppCard(
              child: Row(
                children: [
                  const Icon(Icons.phone, color: AppColors.primary),
                  const SizedBox(width: 12),
                  Expanded(
                    child: Column(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        const Text('联系方式',
                            style: TextStyle(
                                fontSize: 12,
                                color: AppColors.onSurfaceVariant)),
                        Text(phone,
                            style: const TextStyle(
                                fontSize: 16, fontWeight: FontWeight.w700,
                                color: AppColors.onSurface)),
                      ],
                    ),
                  ),
                ],
              ),
            ),
          const SizedBox(height: 16),
          if (status != 'DONE')
            SizedBox(
              width: double.infinity,
              height: 48,
              child: ElevatedButton.icon(
                onPressed: _accepting ? null : _accept,
                icon: _accepting
                    ? const SizedBox(
                        width: 16, height: 16,
                        child: CircularProgressIndicator(
                            color: Colors.white, strokeWidth: 2))
                    : const Icon(Icons.volunteer_activism, size: 18),
                label: Text(_accepting ? '提交中' : '我能帮 TA'),
              ),
            ),
        ],
      ),
    );
  }
}
```

#### 1.3 composer 重写 —— 6 种社交类 + 2 个特殊上报快捷入口

`_openComposer` 改造：

```dart
Future<void> _openComposer() async {
  // 顶部先弹一个小 sheet 让用户选「发布社交动态」or「跳到上报」
  // 简化版：直接在 composer 顶部加一行特殊入口 chip，点击立即跳出
  final titleC = TextEditingController();
  final contentC = TextEditingController();
  final phoneC = TextEditingController();
  var type = '互助求助';

  const types = [
    '互助求助',
    '招工',
    '分享见闻',
    '失物招领',
    '二手交易',
    '闲置共享',
  ];

  final ok = await showModalBottomSheet<bool>(
    context: context,
    isScrollControlled: true,
    backgroundColor: AppColors.surface,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.vertical(top: Radius.circular(R.lg)),
    ),
    builder: (sheetCtx) => Padding(
      padding: EdgeInsets.fromLTRB(
          20, 20, 20, MediaQuery.of(sheetCtx).viewInsets.bottom + 20),
      child: StatefulBuilder(
        builder: (ctx, setSheet) => Column(
          mainAxisSize: MainAxisSize.min,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            const Text('发布动态',
                style: TextStyle(
                    fontSize: 18,
                    fontWeight: FontWeight.w700,
                    color: AppColors.onSurface)),
            const SizedBox(height: 4),
            const Text('选择类型，详细描述后发布到乡村动态广场',
                style: TextStyle(
                    fontSize: 12, color: AppColors.onSurfaceVariant)),
            const SizedBox(height: 14),

            // 特殊上报快捷入口
            const Text('特殊上报',
                style: TextStyle(
                    fontSize: 12, fontWeight: FontWeight.w600,
                    color: AppColors.onSurfaceVariant)),
            const SizedBox(height: 8),
            Row(
              children: [
                Expanded(
                  child: OutlinedButton.icon(
                    onPressed: () {
                      Navigator.pop(sheetCtx, false);
                      context.go('/disaster');
                    },
                    icon: const Icon(Icons.thunderstorm_outlined, size: 16),
                    label: const Text('灾情上报'),
                  ),
                ),
                const SizedBox(width: 10),
                Expanded(
                  child: OutlinedButton.icon(
                    onPressed: () {
                      Navigator.pop(sheetCtx, false);
                      context.go('/agri');
                    },
                    icon: const Icon(Icons.eco_outlined, size: 16),
                    label: const Text('农事记录'),
                  ),
                ),
              ],
            ),
            const SizedBox(height: 16),

            // 社交类
            const Text('动态类型',
                style: TextStyle(
                    fontSize: 12, fontWeight: FontWeight.w600,
                    color: AppColors.onSurfaceVariant)),
            const SizedBox(height: 8),
            Wrap(
              spacing: 8, runSpacing: 8,
              children: [
                for (final t in types)
                  ChoiceChip(
                    label: Text(t),
                    selected: type == t,
                    onSelected: (_) => setSheet(() => type = t),
                  ),
              ],
            ),
            const SizedBox(height: 14),
            TextField(
              controller: titleC,
              decoration: InputDecoration(
                labelText: _titleLabel(type),
                filled: true,
              ),
            ),
            const SizedBox(height: 10),
            TextField(
              controller: contentC,
              maxLines: 4,
              minLines: 3,
              decoration: InputDecoration(
                labelText: _contentLabel(type),
                filled: true,
                alignLabelWithHint: true,
              ),
            ),
            const SizedBox(height: 10),
            TextField(
              controller: phoneC,
              keyboardType: TextInputType.phone,
              decoration: const InputDecoration(
                  labelText: '联系电话（选填）', filled: true),
            ),
            const SizedBox(height: 18),
            SizedBox(
              width: double.infinity,
              height: 50,
              child: ElevatedButton(
                onPressed: () {
                  if (titleC.text.trim().isEmpty) {
                    toast(ctx, '请填写${_titleLabel(type)}', error: true);
                    return;
                  }
                  Navigator.pop(sheetCtx, true);
                },
                child: const Text('发布'),
              ),
            ),
          ],
        ),
      ),
    ),
  );

  if (ok != true || !mounted) return;
  final payload = {
    'type': type,
    'title': titleC.text.trim(),
    'content': contentC.text.trim(),
    'contactPhone': phoneC.text.trim(),
  };
  try {
    await ApiClient.post('/life/help', body: payload);
    if (!mounted) return;
    toast(context, '$type 发布成功');
    _load();
  } catch (_) {
    await OfflineSyncQueue.enqueue(
      tableName: 'help_request',
      payload: payload,
      path: '/life/help',
    );
    if (!mounted) return;
    toast(context, '已加入待发送队列，将自动发布');
  }
}

String _titleLabel(String type) {
  if (type == '二手交易' || type == '闲置共享') return '物品名称';
  if (type == '失物招领') return '失物 / 招领描述';
  if (type == '招工') return '招工岗位';
  if (type == '分享见闻') return '标题';
  return '需要什么帮助';
}

String _contentLabel(String type) {
  if (type == '二手交易') return '物品成色 / 价格 / 取货地点';
  if (type == '闲置共享') return '物品功能 / 借用规则';
  if (type == '失物招领') return '丢失时间 / 地点 / 特征';
  if (type == '招工') return '工作内容 / 时间 / 薪酬';
  if (type == '分享见闻') return '想分享的内容';
  return '详细描述（时间、地点、需要怎么帮）';
}
```

#### 1.4 `_typeColor` 扩充

```dart
Color _typeColor(String type) {
  if (type.contains('招工')) return AppColors.goldContainer;
  if (type.contains('分享')) return AppColors.primaryContainer;
  if (type.contains('互助') || type.contains('求助')) return AppColors.primary;
  if (type.contains('二手') || type.contains('闲置')) return AppColors.secondary;
  if (type.contains('失物')) return AppColors.warning;
  return AppColors.primary;
}
```

### 2. 后端

**不动**。`/life/help` POST 已经接受任意 `type` 字符串字段（参见 `backend/src/modules/life/community.controller.js` 的 `helpCreate`）。

> Codex 注：实施时**确认** `helpCreate` 没有 `type` 白名单。如果有，需要扩白名单到 6 种新 type 值，并跑后端测试。如果没有，跳过。

## 三、接口契约

### 已有

- `GET /life/help/list?pageSize=20` —— feed 列表
- `POST /life/help` —— 创建动态（已支持任意 type 字符串）
- `POST /life/help/:id/accept` —— 响应/接单
- `GET /village/affairs?pageSize=1` —— 村委通知
- `/disaster` / `/agri` —— 特殊上报跳板块

### 新增

无。

## 四、验收

1. `flutter analyze lib/pages/publish` → `No issues found!`
2. 端到端：
   - 进入「发布」tab → 看到 feed 列表
   - 点任一卡片 → 弹底部详情 sheet，看到完整内容 + 作者 + 时间 + 联系电话卡 + 「我能帮 TA」按钮
   - 点「我能帮 TA」→ 二次确认（toast）→ sheet 关闭 + feed 自动刷新，该条状态变「已响应」
   - 已响应的条目点开，看不到「我能帮 TA」按钮
   - 点 FAB「+」→ composer sheet 弹出：
     - 顶部「特殊上报」一行有「灾情上报」「农事记录」两个 OutlinedButton
     - 点「灾情上报」→ sheet 关闭，跳 `/disaster`
     - 点「农事记录」→ sheet 关闭，跳 `/agri`
     - 「动态类型」一栏有 6 个 ChoiceChip：互助求助 / 招工 / 分享见闻 / 失物招领 / 二手交易 / 闲置共享
     - 切换类型时 标题/内容输入框的 labelText 跟着变（如「二手交易」→ 物品名称 / 物品成色...）
     - 填完点发布 → toast 显示「XX 发布成功」+ feed 刷新出新条目
3. 离线场景：拔网后发布 → toast「已加入待发送队列」，feed 不立刻显示，network 回来后自动同步

## 五、不在范围内

- 不做后端 union 把 disaster_report / farm_record / market_product 也展示到 feed
- 不做"我的发布"过滤（feed 显示所有人的）
- 不做"我的"页跳转到「我的发布」的具体列表（44c 已经做了跳转 `/publish`，可接受）
- 不做评论 / 点赞（后端没有相应表）
- 不做图片上传（current `/life/help` POST 不支持 images 字段）
- 不优化 feed 滚动性能（暂时全量加载 pageSize=20，够用）

## 六、关联

- 总览：[46-用户反馈批次.md](46-用户反馈批次.md)
- 同批次姊妹：[46a-我的页与设置页修复.md](46a-我的页与设置页修复.md) / [46b-AI双层重构.md](46b-AI双层重构.md)
- 后端：`backend/src/modules/life/community.controller.js`

## 七、实施备注（Codex 完成后填写）

- `app/lib/pages/publish/publish_page.dart` 已把动态卡片改为可点击详情 sheet，展示类型、状态、标题、作者、时间、完整内容、联系方式，并可通过现有 `POST /life/help/:id/accept` 响应动态。
- Composer 已扩展为 6 种社交类：互助求助、招工、分享见闻、失物招领、二手交易、闲置共享；顶部新增「灾情上报」「农事记录」快捷入口，分别跳转 `/disaster` 与 `/agri`。
- 后端确认：`/life/help` 接收任意 `type` 字符串，没有白名单，本段未新增或修改接口。
- 文案：新增/修改的用户可见文案已按 `docs/产品呈现口径.md` 第五节校对，未使用「离线 / 断网 / 本地兜底」等词。
- 验证：`flutter analyze lib/pages/publish` 通过；`flutter analyze lib` 通过；`node --check backend/src/modules/life/community.controller.js` 通过。
