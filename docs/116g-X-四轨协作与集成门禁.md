# 116g-X — 四轨协作与集成门禁

> 状态：**PR #3 已开、未合并；CI 5/5 全绿；远端 head=`e4b6a16`（已合并 origin/main @ `87cb07f`）**
> 上位计划：[116-大重构总计划.md](116-大重构总计划.md)（M0/M9 工程收口线）；承接 [116f-APIv2与能力注册表.md](116f-APIv2与能力注册表.md) 的生成链与 drift 门禁
> 分支：`collab/integration-gates`（远端已推送，PR #3：https://github.com/xkitme/FarmLink2/pull/3；原 PR #1 已按用户指示关闭）
> 基线：`origin/main` @ `87cb07f38b943a3a1e5e56060a65969f8dbf0026`（= Merge PR #4，含 Flutter analyze 修复）；祖先基线 `5ff3a0117a79553e1125024d225972a61c8c1325`（116f D6 收口）为 HEAD 祖先 ✓
> 实施 head：`e4b6a16dd77e757c4ee40e756dbf8aab789195c8`（含两个 merge commit：并入远端分支历史 + 并入 origin/main；tree 与本地 HEAD 一致）
> 目标：为 Backend、Flutter、Admin、Integration 四个并行分支建立可自动检查的文件所有权、CI 与交接规则，防止多人直接覆盖 main、手改生成产物或把越界改动混入提交；CI 同时按 **lane + workorder 精确白名单**检查当前工单的允许范围。
> 本工作单编号自 116g-A 更名为 **116g-X**（PR #3 标题即 116g-X，已完成；PR #1 已关闭）。

---

## 1. 背景与缺口

116f 完成后，项目进入四轨并行（Backend/Flutter/Admin 三条产品轨 + Integration 集成轨），但缺少：

1. **机器可读的文件所有权**：谁可以改什么路径没有自动检查，越界改动只能靠人工 review。
2. **生成产物保护**：`capabilities.js`、`apiCatalog.js`、`assistant-catalog.generated.js`、`feature_catalog.dart` 等 5 个生成产物没有「只许 Integration 重建」的硬约束。
3. **CI 门禁**：仓库尚无 `.github/workflows`；Backend/Admin/Flutter 的测试与 5 个 drift 门禁没有在 GitHub Actions 独立显示；失败无法自动阻断 PR。
4. **交接约定**：`docs/协作约定.md` 仍是双人（Claude×Codex）模式，未覆盖四轨分支命名、合并顺序、DB/Prisma 安全、截图批准、完整 hash 集成等要求。
5. **PR 模板**：缺失 BASE/HEAD hash、A/M/D、测试闭合、DB 指纹、Prisma diff、drift、截图、回滚等必填字段。
6. **工单级约束缺失（聚焦整改新增）**：仅按 lane 宽泛放行 `backend/src/**`、`app/lib/**`、`docs/**` 会让越界工单混入；必须按分支解析出真实 workorder，再按该工单的**精确白名单**检查。

## 2. 本批目标（对外口径）

把「谁改了什么、能不能改、生成产物是否漂移、CI 是否全绿」全部变成**自动可检查**的规则；任何越界、手改生成产物、脏提交在 CI 上一律失败，人工审查只处理语义问题。CI 的变更范围门禁按「真实分支 → lane + workorder → 工单精确白名单」三层收敛。

## 3. 范围（文件级）

### 3.1 新增

| 文件 | 内容 |
|---|---|
| `scripts/collaboration/lanes.json` | 机器可读配置（schemaVersion=2）：`branchMap`（真实分支 → lane+workorder）、`workorders`（116g-A/116h-A/116g-B/116g-X 精确白名单 + pending 待定项）、四条 lane 的 allow/forbid/exceptions（legacy 用）、5 个生成产物 + globalForbid |
| `scripts/collaboration/scope-check.mjs` | 只读 scope checker（`--branch/--lane/--workorder/--base/--head/--repo/--config/--json`），git diff name-status（A/M/D/R/C…），中文/空格路径无损，越界精确列出并退出非零；未知/模糊分支退出码 2；不改工作树 |
| `scripts/test/scope-check.test.mjs` | scope checker 强测试（node:test；legacy + branchMap + workorder 白名单全覆盖，数量见 §10.2） |
| `scripts/test/package.json` | 测试入口（`npm test --prefix scripts/test`） |
| `.github/workflows/ci-gates.yml` | 四轨协作 CI：scope-check（`--branch` 解析 lane+workorder）、backend-tests、backend-drift（npm ci + 5 个 drift 步骤独立显示）、admin-test-build、flutter-analyze-test（SDK 3.32.1 + 全量 analyze） |
| `.github/pull_request_template.md` | PR 必填模板（BASE/HEAD hash、A/M/D、测试闭合、安全、DB 指纹、Prisma diff、drift 5/5、截图、回滚） |
| `docs/116g-X-四轨协作与集成门禁.md` | 本工作单（自 116g-A 更名） |

### 3.2 修改

| 文件 | 改动 |
|---|---|
| `docs/协作约定.md` | 新增「四轨协作与集成门禁」章节：独立 clone/worktree、分支命名、禁止 push main / force / rebase / reset、分叉停止、生成产物仅 Integration 更新、合并顺序、每批报告字段、DB/Prisma 安全、截图批准门禁、完整 hash 集成、越界处理；116g-A 链接更新为 116g-X；矩阵补充 workorder 白名单说明 |

### 3.3 不改动（本批明确不碰）

- `scripts/verify-all.ps1`（保持 16/16 口径）
- `backend/src/**`、`backend/admin/src/**`、`app/lib/**`、`app/test/**`（业务代码，本批零改动）
- Prisma schema/migrations；正式数据库 `backend/data/village.db`
- 注册表人工事实源与 5 个生成产物
- `docs/进度总览.md`、`docs/claude-memory.md`
- 远端操作：不 amend / rebase / reset / force push；不合并 PR #3；不改 GitHub 设置

## 4. 实施内容与验收

### 4.1 lane + workorder 配置（lanes.json，schemaVersion=2）

- `branchMap`（真实分支 → lane + workorder）：
  - `collab/116g-backend-*` → backend / **116g-A**
  - `collab/116h-flutter-*` → flutter / **116h-A**
  - `collab/116g-admin-*` → admin / **116g-B**
  - `collab/integration-*` → integration / **116g-X**
- `workorders` 精确白名单：
  - **116g-A（backend）**：`backend/test/**`、`backend/package.json`、`backend/package-lock.json`、`docs/116g-A-*.md` + **8 个精确 controller/policy（2026-08-22 阶段 0 已回填）**：`backend/src/modules/market/order.policy.js`、`order.controller.js`、`backend/src/modules/data/sync.policy.js`、`sync.controller.js`、`backend/src/modules/machinery/booking.policy.js`、`booking.controller.js`、`backend/src/modules/platform/resource.policy.js`、`resource.controller.js`；**`backend/src/**` 其余路径仍拒绝（不宽泛放行）**，pending=0。
  - **116h-A（flutter）**：`app/lib/main.dart`、`app/lib/core/theme.dart`、`app/lib/core/router.dart`、`app/lib/core/elder_mode.dart`（**2026-09-01 白名单补齐**）、`app/lib/design_system/**`、`app/lib/pages/home/shell_page.dart`、`app/lib/pages/home/home_page.dart`、`app/lib/pages/splash/splash_page.dart`（**2026-09-01 白名单补齐**）、`app/lib/widgets/common.dart`、`app/test/**`、`app/pubspec.yaml`、`app/pubspec.lock`、`docs/116h-A-*.md`（依据 116-大重构总计划 M4 重点文件 + collab/116h-flutter-shell-polish 实测路径；feature_catalog.dart 仍由生成产物硬拒）。
  - **116g-B（admin）**：`backend/admin/src/api/**`、`backend/admin/src/App.jsx`、`backend/admin/src/layouts/**`、`backend/admin/src/pages/**`、`backend/admin/src/components/**`、`backend/admin/src/policies/**`、`backend/admin/src/styles.css`、`backend/admin/test/**`、`backend/admin/package.json`、`backend/admin/package-lock.json`、`docs/116g-B-*.md`（白名单覆盖远端分支 collab/116g-admin-safety @ c580d57 的真实 18 个文件）。
  - **116g-X（integration）**：`.github/**`、`scripts/collaboration/**`、`scripts/test/**`、`docs/协作约定.md`、`docs/116g-X-四轨协作与集成门禁.md`（**docs 不再宽泛放行**）。
- 判定顺序（workorder 模式）：globalForbid → 生成产物（非 integration lane）→ lane.forbid → 工单白名单（pending 永不放行）→ 越界。

**验收**：配置可被 scope-check.mjs 读取；116g-B 白名单覆盖真实分支 18 文件（测试 C6）；116g-A pending=0 且 8 个精确路径放行、其余 backend/src 拒绝（测试 C8/C9）。

### 4.2 scope checker（scope-check.mjs）

- 参数：`--branch`（推荐，经 branchMap 解析 lane+workorder）、`--lane`+`--workorder`（显式工单模式）、`--lane`（legacy）；`--base` 必填、`--head` 默认 HEAD、`--repo`、`--config`、`--json`。
- 变更来源：`git diff -z --name-status -M <base> <head>`，覆盖 A/M/D/R/C/T 等；`core.quotepath=false` + NUL 分隔，正确处理空格、中文路径；重命名同时检查新旧路径。
- 失败即非零：越界（退出 1，精确列出 路径+状态+原因）；分叉（base 非 head 祖先，退出 1）；脏工作树（退出 1）；未知 lane / 缺 base / sha 不可解析 / **未知分支 / 模糊分支 / --branch 与 --lane/--workorder 冲突 / 配置不一致**（退出 2）。
- 禁止把 generated 白名单放给业务 Agent（代码内硬编码生成产物检查）。
- checker 本身只读：不写文件、不改 .git、不碰工作树。

**验收**：`node --test scripts/test/scope-check.test.mjs` 全绿（数量见 §10.2）；四真实分支解析正确；未知/模糊分支非零。

### 4.3 CI（ci-gates.yml）

- 触发：`pull_request` + `workflow_dispatch`；`permissions: contents: read`（最小化，无可写 token、不读 secrets、不上传任何产物）。
- `concurrency` 按 ref 分组 + `cancel-in-progress: true`；每个 job 有 `timeout-minutes`。
- **Node 版本固定项目基线 `24.16.0`**（`env.NODE_VERSION`，不用浮动 26）。
- Job 拆分独立显示：scope-check（`--branch` 解析 lane+workorder，未知/模糊分支失败）、Backend tests（npm ci + npm test）、Admin test+build、Flutter analyze+test（SDK `3.32.1`，`flutter analyze` 全量含 test 目录）、backend-drift（**先 `npm ci --prefix backend` 装锁定依赖，再 5 个 drift 步骤独立显示**）。
- 任一 job/step 失败必须传播为 workflow 失败（无 continue-on-error、无 `|| true`、无空 job）。
- 第三方 action 全部固定 commit SHA：checkout `11bd7190...`（v4.2.2）、setup-node `49933ea5...`（v4.4.0）、flutter-action `1a449444...`（v2.23.0）。
- CI 不写 `backend/data/village.db`；CI 测试库为临时合成库（沿用 backend 测试既有模式），不留库文件。
- 本地 verify-all（依赖本机 Flutter 绝对路径）无法直接进 CI → **不降级 verify-all**，拆出等价的 CI 子门禁，差异见 §8。
- **不改生成产物来掩盖 CI 配置缺陷**：drift 门禁失败必须回查生成器/配置，禁止手改产物。

**验收**：actionlint + PyYAML 静态校验通过；每 job 权限/超时/失败传播符合上述；无 artifact、无写 token。

### 4.4 PR 模板

必须包含：BASE/HEAD 完整 hash（base 必须是 head 祖先）；A/M/D 精确清单；测试计数闭合（tests/suites/文件数，禁 exit code 代替）；安全影响；DB 指纹（SHA256/Size/Mtime）；Prisma diff；5 个生成器 `--check` 结果；截图（UI 变更必填）；回滚方式；自检清单。

### 4.5 协作约定更新

见 §3.2；覆盖独立 worktree、分支命名、禁止 push main、禁止 force/rebase/reset、分叉停止、生成产物仅 Integration 更新、合并顺序、每批报告字段、DB/Prisma 安全、截图批准门禁、集成负责人只按已审查完整 hash 集成。

## 5. CI 真实性要求

1. 不用 continue-on-error / `|| true` / 空 job 伪造通过。
2. verify-all 依赖本机绝对路径（Flutter 需本机安装）无法直接用于 GitHub Actions → 不降低 verify-all，拆等价 CI 子门禁（§4.3），报告列出本地与 CI 差异及原因（§8）。
3. CI 不写 village.db。
4. 测试数据库必须为临时合成库并清理。
5. 不修改 verify-all 16/16 口径（本批零改动）。
6. 不修改生成产物来掩盖 CI 配置缺陷。

## 6. 不在范围内

- 不合并 PR #3、不改 GitHub 设置（分支保护、secret、webhook）；不 force-push、不 amend/rebase/reset；不修改 app/backend/admin 业务源码。
- 不实施任何业务功能（后端 controller/service、Flutter 页面、Admin 页面、Prisma、数据库）。
- 不修改 5 个生成产物、注册表事实源、`docs/进度总览.md`、`docs/claude-memory.md`。
- 不收集、不合并其他三人（Backend/Flutter/Admin）的分支；不接入 QA（QA 按既定规则在 Integration 合并后另行授权）。
- 不修改 verify-all.ps1。

## 7. 四轨允许/禁止路径矩阵（速查，完整 glob 以 lanes.json 为准）

| workorder（分支） | 工单精确白名单（allow） | 硬禁止 | 待定 |
|---|---|---|---|
| 116g-A（collab/116g-backend-*） | backend/test/**、backend/package.json、backend/package-lock.json、docs/116g-A-*.md + **8 个精确 controller/policy（已回填，见 §4.1）** | 5 生成产物、backend/data/**、Prisma | 无（pending=0；其余 backend/src/** 仍拒绝） |
| 116h-A（collab/116h-flutter-*） | app/lib/main.dart、core/theme、core/router、**core/elder_mode（已补齐）**、design_system/**、pages/home/shell_page、pages/home/home_page、**pages/splash/splash_page（已补齐）**、widgets/common、app/test/**、pubspec.yaml、pubspec.lock、docs/116h-A-*.md | 5 生成产物（含 feature_catalog.dart）、backend/** | 无 |
| 116g-B（collab/116g-admin-*） | backend/admin/src/{api,App.jsx,layouts,pages,components,policies,styles.css}、admin/test/**、admin/package*.json、docs/116g-B-*.md | 5 生成产物（含 apiCatalog.js）、app/**、backend/src/** | 无 |
| 116g-X（collab/integration-*） | .github/**、scripts/collaboration/**、scripts/test/**、docs/协作约定.md、docs/116g-X-四轨协作与集成门禁.md | backend/data/**、Prisma、业务代码 | 无 |

> 注：legacy `--lane` 模式仍保留宽泛 lane 允许路径（本地 ad-hoc 用），**CI 一律走 workorder 白名单**。

## 8. 本地 verify-all 与 CI 门禁映射

| 本地 verify-all 步骤（16/16） | CI 对应 |
|---|---|
| Flutter pub/analyze/test/build web | `flutter-analyze-test`（SDK 3.32.1；analyze 全量含 test；本机无 Flutter → 本地该步失败，CI 用 flutter-action 固定 SHA 安装） |
| Backend node --check / tests | `backend-tests`（npm ci + npm test；node --check 由 scope-check job 的语法自检 + drift job 运行覆盖新脚本） |
| 116f inventory/registry drift（2 子门禁） | `backend-drift` steps 1/2（npm ci 后） |
| 116f generated catalog drift（3 子门禁） | `backend-drift` steps 3/4/5 |
| Admin test / build | `admin-test-build` |
| village.db 指纹保护 | 本批新增 CI 不运行正式库（无 DB）；本地验证中指纹核对照常 |
| —（verify-all 无此步） | `scope-check`（变更范围门禁，CI 独有） |

本地限制：本机未安装 Flutter（`C:\dev\flutter`、`C:\flutter`、PATH 均无）→ `verify-all.ps1` 的 toolchain: Flutter 步失败；其余非 Flutter 步骤为本地真实验证。这是环境差异，不是门禁降级。Node 版本：CI 固定 24.16.0（项目基线）；本机验证用 26.7.0 与便携 24.16.0 双跑（见 §10.2）。

## 9. 回滚

| 改动 | 回滚 |
|---|---|
| lanes.json / scope-check.mjs / 测试 | 删除/还原 `scripts/collaboration/`、`scripts/test/` |
| ci-gates.yml / PR 模板 | 删除 `.github/workflows/ci-gates.yml`、`.github/pull_request_template.md` |
| docs/协作约定.md 新增章节 | 按 git diff 撤销追加段 |
| 本工作单（更名） | `git mv docs/116g-X-四轨协作与集成门禁.md docs/116g-A-四轨协作与集成门禁.md`（如需还原编号） |

本批全部为新增文件 + 单文档追加，回滚零风险；不触碰任何业务代码与数据库。

## 10. 实施备注

> 2026-08-17 首轮（116g-A）：8 文件新增/追加，commit `038b142` 已推送，PR #1 已创建。
> 2026-08-17 聚焦整改：更名 116g-X + branchMap/workorder 白名单 + CI 修正（Node 24.16.0、drift 装依赖、Flutter 固定 SDK 与全量 analyze），commit `087ef08`；用户指示关闭 PR #1 并重开 → 经 Git Data API 推送（远端 `ef8cff7`，tree 与本地一致），PR #1 关闭、**PR #3 创建（未合并）**。
> 2026-08-18 本轮整改（PR #3 上）：scope-check job 加入 `npm test --prefix scripts/test`（45 用例进入远端 CI）+ setup-node 24.16.0；PR body A/M/D/R 修正为与 GitHub 实际 diff 一致（7 A + 1 M，无 D/R）；文档过期口径更新；推送后 PR #3 head 更新。
> 2026-08-22 合并前口径修正：PR #4 合入 main（`87cb07f`）后，本分支合并 origin/main（head `e4b6a16`，两个 merge commit，无冲突）；**PR #3 CI 5/5 全绿**（含 flutter analyze+test 转绿）；文档/PR body 基线更新为 main@`87cb07f`；等待总审查最终合并指令。
> 2026-08-22 PR #3 合入 main（`39f4ccd`），PR #5 合入（`34e2070`）；统一基线 main=`34e2070`。**116g-A 阶段 0 白名单回填（本 PR）**：Backend 路径申请审查通过，lanes.json 中 116g-A pending 8→0，8 个精确 controller/policy 入 allow；测试 C8/C9 更新锁定「8 路径放行、其余 backend/src 拒绝」；不宽泛放行、不改 backend 业务源码。
> 2026-09-01 **116h-A 白名单补齐（elder_mode + splash 真实路径）**：collab/116h-flutter-shell-polish（head `a62b696`，scope-check 基线仅 2 条 violation）实测路径审查通过，lanes.json 116h-A allow 增补 `app/lib/core/elder_mode.dart`、`app/lib/pages/splash/splash_page.dart`（精确路径，不宽泛放行 app/lib/**）；测试新增 C12（修改两路径放行，配置级+功能级双锁定）/C13（backend/admin/scripts/.github/feature_catalog 仍拒绝），45→47；协作约定矩阵同步。

### 10.1 交付物

| 文件 | 状态（本轮） |
|---|---|
| `scripts/collaboration/lanes.json` | 修改（schemaVersion=2：+branchMap、+workorders、+pending） |
| `scripts/collaboration/scope-check.mjs` | 修改（+--branch、workorder 白名单模式、分支歧义检测） |
| `scripts/test/scope-check.test.mjs` | 修改（新增 B/C/D 组测试） |
| `scripts/test/package.json` | 不变 |
| `.github/workflows/ci-gates.yml` | 修改（Node 24.16.0、backend-drift npm ci、flutter 3.32.1 + 全量 analyze、scope-check --branch） |
| `.github/pull_request_template.md` | 不变 |
| `docs/协作约定.md` | 修改（116g-A → 116g-X 链接、矩阵补 workorder 白名单说明） |
| `docs/116g-A-四轨协作与集成门禁.md` | **更名 → `docs/116g-X-四轨协作与集成门禁.md`**（已于 087ef08 提交推送） |

### 10.2 验证结果（聚焦整改，全部实测）

| 验证项 | 结果 |
|---|---|
| scope checker 测试 | 数量与结果见下方「测试数量闭合」；覆盖：legacy 25 项 + branchMap（4 真实分支/未知/模糊/冲突）+ workorder 白名单（116g-X 正负、116g-B 真实 18 文件全覆盖、**116g-A 回填后 8 路径放行 + 其余 backend/src 拒绝（C8/C9）**、116h-A 正负）+ workorder 模式 A/M/D/R 与中文/空格路径 |
| workflow 静态校验 | PyYAML 解析通过；**actionlint 1.7.7 → exit 0**；无 continue-on-error / `\|\| true` / upload-artifact / secrets / GITHUB_TOKEN |
| 五个 drift 门禁 | Node 24.16.0（便携版）下 `--check` → **5/5 通过**（242/242、247、11 组 242 条、34 页/16 页、8 section/70 功能点）；本地 Node 26.7.0 同 5/5 |
| Backend/Admin 可执行检查 | Backend `npm test` → 224/224（13 suites）；Admin `npm test` → 39/39（4 suites）+ `npm run build` 通过（Node 24.16.0 双跑） |
| Flutter | 本机无 Flutter；CI 配置固定 SDK 3.32.1 + `flutter analyze`（全量）+ `flutter test`，未改 Flutter 源码。**PR #3 当前 CI：Flutter analyze + test 通过**（PR #4 已把 6 个存量 `app/test/` analyze 问题修复并入 main，本分支合并 origin/main 后门禁转绿） |
| `git diff --check` | exit 0 |
| DB/残留/Prisma | `backend/data/village.db` 不存在（gitignore）；`backend/prisma/.test-*` 残留 0；全仓库额外 DB 文件 0；Prisma diff（5ff3a01..HEAD + 工作树）空 |
| 生成产物核对 | 5 个生成产物工作树与 index 一致，未被本批修改；drift 失败未用改产物掩盖 |

**测试数量闭合**：`node --test scripts/test/scope-check.test.mjs` → tests **47 / pass 47 / fail 0**（A 组 legacy 25 + B 组 branchMap 5 + C 组 workorder 白名单 13 + D 组 workorder 模式 A/M/D/R 与中文路径 4）。2026-09-01 116h-A 白名单补齐后新增 C12（elder_mode/splash 修改放行，配置级+功能级双锁定）/C13（backend/admin/scripts/.github/feature_catalog 仍拒绝），45 → 47。

### 10.3 本地/CI 差异与说明

1. **EOL 环境差异（本机修复，非仓库改动）**：本机全局 `core.autocrlf=true` 使检出文件为 CRLF，而生成器与提交产物为 LF → 五个 drift 门禁初始失败。修复：仓库本地 `core.autocrlf=false` + 5 个生成产物重刷 LF。CI（Linux）默认无 autocrlf，不受影响。
2. **Node 版本**：CI 固定 `24.16.0`（项目基线，用户指定）；本机默认 26.7.0。已下载便携 Node 24.16.0 双跑 drift 门禁与 scope 测试/Backend/Admin 检查，确认 24.16.0 下全部通过（与 26.7.0 结论一致）。
3. **verify-all 与 CI 映射**：本地 Flutter/Java 缺失导致 verify-all exit 1 属环境差异，非门禁降级；等价 CI 子门禁见 §8。
4. **CI 生效时机**：workflow 随 PR 合并 ref 运行；本轮整改推送后 PR #3 触发 synchronize 重跑，scope-check job 将执行 45 个 scope checker 测试（`npm test --prefix scripts/test`）后再跑变更范围检查。
5. **Flutter analyze 全量门禁（已转绿）**：`flutter analyze` 全量（含 test）首次运行暴露 6 个存量 app/test/ analyze 问题；PR #4 修复并入 main（87cb07f）后，本分支合并 origin/main（e4b6a16）重跑 CI，**Flutter analyze + test 通过**，PR #3 CI 5/5 全绿。
6. **116g-A pending 说明（已回填）**：116g-A 阶段 0 路径申请经审查后，Integration 将 8 个精确 controller/policy 回填为 allow（pending 8→0）；`backend/src/**` 其余路径仍拒绝。C8（8 路径放行）/C9（其余路径拒绝）锁定该语义。

### 10.4 明确未做（本轮）

- 未修改任何业务代码（app/backend/admin 业务源码）、生成产物、注册表事实源、Prisma、数据库、`docs/进度总览.md`、`docs/claude-memory.md`、`scripts/verify-all.ps1`。
- 未收集或合并 Backend/Flutter/Admin 其他分支（PR #2 为 Admin 协作者产物，未触碰）；未接入 QA。
- 未 force-push、未 amend/rebase/reset、未合并 PR #3。

### 10.5 状态

分支 `collab/integration-gates`：本地 HEAD=`e4b6a16`（= 远端 head，工作树干净），已合并 origin/main @ `87cb07f`；PR #3 open 未合并，**CI 5/5 全绿（scope-check 含 45 用例测试、backend-drift 5/5、backend-tests、admin-test-build、flutter analyze+test）**。工作单/PR 状态以远端为准；合并等待总审查最终指令。
