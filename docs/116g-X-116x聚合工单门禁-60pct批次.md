# 116g-X — 116x-60pct 聚合工单门禁（集成 lane）

> 状态：实施完成（scope checker 测试 47 → 49 全绿；聚合工单精确白名单落地）
> 承接：[116g-X-四轨协作与集成门禁.md](116g-X-四轨协作与集成门禁.md)（原始四轨门禁，PR #3）
> 分支：`collab/116x-60pct-integration` → workorder **116x-60pct**（branchMap 新增 `collab/116x-*`）
> 基线：`origin/main` @ `dddfd6abc352f4f18f18737f57fc1b77937d8d9a`

## 1. 背景

60% 重构整合主线的单 PR 同时触碰 Backend / Flutter / Admin / Integration 四 lane 文件，
既有的 branchMap（116g-backend-* / 116h-flutter-* / 116g-admin-* / integration-*）无法映射
`collab/116x-*` 前缀。按「聚合工单精确白名单、不宽泛放行」原则新增 workorder。

## 2. 改动（Integration lane，全部在 116x-60pct / 116g-X 白名单内）

| 文件 | 改动 |
|---|---|
| `scripts/collaboration/lanes.json` | branchMap 新增 `collab/116x-*` → lane=integration / workorder=`116x-60pct`；workorders 新增 `116x-60pct`（allow = 本次四 lane 精确授权路径并集：8 个 controller/policy、backend/test/**、backend/package.json、docs/116g-A-*.md、Flutter 5 个精确文件 + app/test/** + pubspec* + docs/116h-A-*.md、Admin policies/pages/tests/package + docs/116g-B-*.md、.github/**、scripts/collaboration/**、scripts/test/**、docs/协作约定.md、docs/116g-X-*.md、pull_request_template.md；pending=0） |
| `scripts/test/scope-check.test.mjs` | B1 真实分支映射 +1（116x-60pct-integration → integration/116x-60pct）；C14 聚合工单正负对照（精确授权文件通过；生成产物/DB/Prisma/其余 backend/src 与 app/lib/宽泛 docs 拒绝）；C15 integration lane 也不得手改生成产物（不在 allow 即拒） |

## 3. 语义（与 116g-X 原始规则一致）

- 判定顺序不变：globalForbid → 生成产物（integration lane 由工单 allow 兜底拒绝）→ lane.forbid → workorder 白名单 → 越界；
- 116x-60pct 的 allow **不含** 5 个生成产物、`backend/data/**`、Prisma、`backend/src/**` 其余路径、
  `app/lib/**` 其余路径、`backend/admin/src/resourceGroups.js`、宽泛 `docs/**`——与 116g-B 文档禁区一致；
- 旧 backend 错基线（b42cf30 删 `.github/scripts/admin` 等行为）不因 `collab/116x-*` 放行：生成产物与
  未授权路径一律按「不在工单白名单」拒绝，`.github/**` 仅放行本批实际改动文件；
- pending=0：116x-60pct 不再有「待回填路径」挂起。

## 4. 验收

1. `npm test --prefix scripts/test` → **49/49**（基线 47 + C14/C15 新增 2）。
2. `node scripts/collaboration/scope-check.mjs --branch collab/116x-60pct-integration --base <base> --head <head> --json` → ok=true（见聚合验证文档）。
3. CI scope-check job 以 PR head 分支名自动命中 116x-60pct 白名单。

## 5. 不在范围内

- 未放宽任何既有 workorder（116g-A/116h-A/116g-B/116g-X 的 allow/pending 均未改动）；
- 未改 `.github/workflows/ci-gates.yml`（job 逻辑本就按分支名解析，无需随 workorder 增改）；
- 未改生成器 / 生成产物；未改 `verify-all.ps1`。

## 6. 回滚

从 lanes.json 删除 `116x-60pct` workorder 与 branchMap 条目、还原 scope-check.test.mjs 即可；
无数据库 / Prisma / 生成产物改动。
