# 68 · GitHub Issues 第二轮处理计划（总览）

> 拉取时间 2026-06-09。当前 OPEN 5 条：#16 #17 #18 #19 #20。
> 本文件是总览级 plan，给出**分诊 / 优先级 / 并行轨 / 待拍板项**；
> 拍板后再拆成各自的 `docs/NN-*.md` 工作单交 Codex/Claude 实施。
> 对外口径仍是「在线 SaaS 平台」（`docs/产品呈现口径.md`）。

## 一、分诊（按"已做多少"分两类）

### A. 代码侧基本完成，只差 release 复验后关账

| Issue | 内容 | 现状（据 docs/64~66 + 交接簿） | 建议 |
|---|---|---|---|
| **#16** 全局优化2期 | ① 动画不流畅 ② 所有卡片改独立页 | ① 已诊断＝**debug 产物**，release 转场/滚动 0 掉帧（仅灾害重页首访 ~85ms 顿挫，优先级低）；② 展示型卡片已迁 4 批（60~63），剩订单/购物/表单等**操作型弹层按约定保留** | **release 真机复验 → 能关就关**；若要再抠，只做灾害重页首帧拆分 |
| **#17** AI 问题1期 | ① 识别不了图片 ② markdown `***` ③ 数据库不全 / 整段无流式感 | ① 识图已重做（真识别＋诚实兜底＋植物门＋反馈＋监控，批 64）；② `_MarkdownText` 加粗渲染已修；③ KB 补到 94 病害＋48 农事历，逐字流式已有 | **release 复验这 3 条 → close** |

> 这两条不该再大改。安排**一次 release 真机走查**（按记忆 `feedback-verify-flutter-in-browser`：清 SW＋CDP 清 HTTP 缓存，跑真 release bundle），过了就让用户 close。

### B. 新功能需求（5/27 之后新提，需真正规划）

| Issue | 内容 | 可行性关键 |
|---|---|---|
| **#18** 输入框加语音输入 | 文本输入框旁加麦克风，语音转文字 | 后端 `/ai/voice/recognize` **是 stub**（回显 text 或返回固定 transcript，**无真 ASR**）。真离线 ASR 在 8G 显存＋比赛机＋无 Docker 下不现实 |
| **#19** 物联网设备联动 | IoT 设备联动 | 离线单机**做不了真 IoT**。只能做**模拟设备**（造传感器数据＋定时更新＋告警联动）。范围弹性大 |
| **#20** 适老模式＋方言语音入口 | 适老模式 + 方言语音 | `isElderMode` 字段**已存在**（user model）。适老＝大字号/高对比/简化 UI，纯前端**可做**；方言语音＝与 #18 同源，依赖 ASR |

## 二、待用户拍板（决定后才好开工作单）

1. **#18 语音输入走哪条路？**
   - **方案 A · 真 STT**：Flutter `speech_to_text` 插件用系统语音识别 → 体验真，但**多半要联网**（安卓 STT 依赖 Google 服务），跟"离线可跑"冲突，比赛机环境要确认。
   - **方案 B · demo 级**：麦克风按钮录音 → 上传走现有 stub → 返回（可改成更像样的多候选文案）。**离线能跑、能演**，但不是真识别。
   - 建议：**比赛 demo 用 B**（稳、离线、可控），文档注明"语音适配层"；若比赛机确认有网且允许，再上 A。
2. **#19 IoT 做到哪一步？**（不做 / 仅"模拟设备看板"展示 / 模拟设备＋告警联动到灾害板块）。建议**仅做模拟设备看板**作为亮点演示，别投入做真协议。
3. **#16/#17 现在就安排 release 复验关账，还是先推新功能？**

## 三、优先级与并行轨（文件域分离）

> ⚠️ 当前占用：**用户**在改 `ai_chat_page / ai_threads_page / disaster_page / shell_page / market_page / core/api_client`；**Codex(67)** 在改 `agri / machinery / policy / life` 页。新轨要避开这些，或等其落地。

| 轨 | 内容 | 依赖 | 谁做 |
|---|---|---|---|
| **T1 关账复验** | #16 + #17 一次 release 真机走查清单 | 无（纯验证） | Claude 跑浏览器/真机 |
| **T2 适老模式（#20a）** | `isElderMode` → 全局字号/控件放大 + 高对比；入口在设置页 | 触 `theme.dart`/设置页（避开用户 6 文件） | 拍板后 Claude 出工作单 |
| **T3 语音输入（#18）** | 选定方案后：输入框组件 `AppSearchField` 加麦克风态 + 录音/STT 接入 | 待**决策 1** | Codex 实施 |
| **T4 方言语音入口（#20b）** | 复用 T3 能力 + 方言提示文案 | 依赖 T3 | 接 T3 |
| **T5 模拟 IoT（#19）** | 后端造设备/读数表 + 定时更新；App 设备卡片 + 可选告警联动 | 待**决策 2** | 后端 Claude + 前端 Codex 两轨 |

## 四、建议执行顺序

1. **先关账**：T1 release 复验 #16/#17（清掉 2 条 open，最快见效）。
2. **快赢**：T2 适老模式 #20a（字段已有、纯前端、不依赖 ASR）。
3. **拍板后**：T3 #18 语音（定方案 A/B）→ T4 #20b 方言入口复用。
4. **大件**：T5 #19 模拟 IoT（定范围后排）。

## 五、不在范围内 / 风险

- 真 IoT 协议、真离线大词表 ASR、真方言声学模型——**单机 8G＋无 Docker＋比赛机**下不做。
- 一切走 stub/模拟的功能，UI 文案不得出现"离线/本地兜底"等词（产品口径）。
- 动画"卡"基本是 debug 误会，别为它大改转场代码。
