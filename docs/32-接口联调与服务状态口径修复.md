# 分段 32 — 接口联调与服务状态口径修复

## 本次目标

延续分段 31 的产品呈现口径，对 Flutter 数据看板、Ant Design Pro 运营驾驶舱、后端数据与 AI 状态接口做一次联调回扫，确保 API 调试器与用户界面展示一致，统一呈现为正式平台服务能力。

## 已完成内容

1. 回看 `docs/进度总览.md`、`docs/设计参考.md` 与 `docs/产品呈现口径.md`，确认后续开发继续遵循 Agro-Modernist Tech 视觉体系与在线 SaaS 成品口径。
2. 巡检 Flutter 端 `ApiClient` 调用与后端模块路由，确认 51 个高频 GET 接口在登录态下均可正常返回。
3. 调整 `backend/src/modules/data/dashboard.controller.js`：
   - 数据看板返回字段由技术状态改为 `serviceStatus`。
   - 遥感诊断返回字段改为 `analysisType: "NDVI 长势诊断"`。
4. 调整 `app/lib/pages/data/data_dashboard_page.dart`：
   - 数据看板首页读取 `serviceStatus.message`。
   - 保持原有视觉样式与刷新交互不变。
5. 调整 `backend/admin/src/pages/Dashboard.jsx`：
   - 服务状态卡片改为“数据源 / 运行状态 / AI”三类业务标签。
   - 不再展示数据库实现、运行模式等内部技术字段。
6. 调整 `backend/admin/src/pages/ApiSwitchPage.jsx`：
   - API 开关页状态标签改为“平台数据服务”。
7. 调整 AI 接口返回：
   - `/ai/status` 改为 `serviceMode` 与 `capability`。
   - 问答、语音、图像识别结果统一使用 `serviceMode`、`serviceStatus` 等平台口径字段。
   - 保留 `modelUsed` 便于运维排查，但避免把内部降级路径作为对外状态展示。
8. 调整后端启动日志：
   - Ollama 未连接提示改为“平台知识库与规则服务可用”，避免开发控制台也出现不一致口径。

## 涉及文件

- `backend/src/modules/data/dashboard.controller.js`
- `backend/src/modules/ai/ai.controller.js`
- `backend/src/modules/ai/services/ask.service.js`
- `backend/src/server.js`
- `backend/admin/src/pages/Dashboard.jsx`
- `backend/admin/src/pages/ApiSwitchPage.jsx`
- `app/lib/pages/data/data_dashboard_page.dart`
- `docs/进度总览.md`

## 验证结果

- 后端 JS 语法检查：通过，`node --check backend/src/**/*.js ok`
- Flutter 静态检查：通过，`flutter analyze` 无问题
- Flutter 单元测试：通过，`flutter test` 全部通过
- 管理台构建：通过，`npm run build` 成功；Vite 仅提示 chunk 体积较大
- 关键接口联调：通过，临时端口登录后请求 `/data/dashboard`、`/ai/status`、`/data/remote-sensing`、`/ai/chat`，4/4 成功
- 文案回扫：通过，用户可见代码与 README 未命中禁止口径

## 当前结论

分段 32 已完成代码层修复，后续继续做剩余板块体验优化、表单细节与联调补强。
