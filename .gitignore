.runtime/
.tools/
dist/

# Claude Code 本机配置（启用插件 / 本地权限），机器相关不进库
.claude/settings.json
.claude/settings.local.json

# 宣传片生成项目的大二进制产物（字体/渲染视频/缩略图缓存）不进主库
promo-video/node_modules/
promo-video/renders/
promo-video/assets/fonts/
promo-video/.thumbnails/

# ── 上传策略：GitHub 仓库只放 docs 与开发相关源码 ──
# 临时验证截图 / 探针图（根目录散落的 png 一律不进库）
/*.png

# Claude / 工具链本机产物（机器相关，不进库）
.agents/
.cdp/
.claude/
.playwright-mcp/
skills-lock.json
/package.json

# 工作脚本与中间产物
outputs/

# 原型 / 设计素材（产品图、图标导出，不上传）
design_assets/
icons_svg/
icons_svg.zip
app/assets/images.zip

# 比赛 PPT / 路演材料（本地保留，不上传）
市赛PPT/
市赛路演-全新/

# 本地 Kokoro TTS：源码(server/requirements)进库，venv/模型/产物不进
tts/.venv/
tts/*.onnx
tts/*.bin
tts/*.wav
tts/*.log
tts/__pycache__/

# 本地 TTS 试听样本（临时，不入库）
tts_samples/

# 离线 STT 模型下载包（487MB tar，本地解压后进 app/assets，原包不入库）
_asrdl/
docs.zip

# 离线 STT 模型权重：int8 encoder 173MB 超 GitHub 单文件 100MB 硬上限，
# 随交付包/本地解压保留，不入库（tokens.txt 小且标识模型，照常入库）。
# 解压：tar -xjf _asrdl/model_full.tar.bz2 --strip-components=1 -C app/assets/models/asr <四个文件名>
app/assets/models/asr/*.onnx
