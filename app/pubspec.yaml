name: farmlink
description: 田园通 FarmLink — 数字乡村助农 App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  go_router: ^14.2.0
  shared_preferences: ^2.3.2
  flutter_animate: ^4.5.0
  cached_network_image: ^3.4.1
  image_picker: ^1.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
