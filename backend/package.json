{
  "name": "farmlink-backend",
  "version": "1.0.0",
  "description": "田园通 — 数字乡村助农 App 后端 + 管理面板",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev":          "concurrently -n backend,admin -c bgBlue,bgGreen \"nodemon src/server.js\" \"npm run dev --prefix admin\"",
    "dev:backend":  "nodemon src/server.js",
    "dev:admin":    "npm run dev --prefix admin",
    "dev:tts":      "node scripts/start-tts.mjs",
    "start":        "npm run build:admin && npm run db:deploy && cross-env NODE_ENV=production node src/server.js",
    "build:admin":  "npm run build --prefix admin",
    "db:generate":  "prisma generate",
    "db:migrate":   "prisma migrate dev",
    "db:deploy":    "node scripts/migrate-safe.mjs",
    "db:push":      "prisma db push",
    "db:seed":      "node seeds/index.js",
    "db:studio":    "prisma studio",
    "db:reset":     "prisma migrate reset",
    "test":         "node --test test/auth-security.test.js test/auth-session.integration.test.js test/c2b-cookie-auth.integration.test.js test/proxy-trust.test.js test/market-trade.test.js test/ai-chat-isolation.test.js test/village-role-isolation.test.js test/contract-registry.test.js test/contract-v2-skeleton.test.js test/contract-v2-market.test.js test/registry-derive.test.js test/order-consistency.test.js test/sync-consistency.test.js test/booking-consistency.test.js test/resource-domain-guard.test.js test/site-carousel.test.js",
    "install:all":  "npm install && npm install --prefix admin"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-validator": "^7.2.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "node-cache": "^5.1.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.7",
    "prisma": "^5.22.0"
  },
  "optionalDependencies": {
    "sharp": "^0.33.0"
  }
}
